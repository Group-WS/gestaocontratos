# Copie este arquivo para .env e preencha com o token NOVO
# (gerado depois de revogar o que foi colado no chat).
# NUNCA commite o arquivo .env de verdade no git.

MONDAY_API_TOKEN=cole_aqui_o_token_novo
PORT=3001
